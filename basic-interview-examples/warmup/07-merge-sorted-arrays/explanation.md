# Explanation

Compare `a[i]` and `b[j]`, push the smaller, advance that index. When one array is exhausted, copy the rest of the other.

**Time:** O(n + m) · **Space:** O(n + m) for output

Same pointer idea as merging billing periods and sliding windows later.
