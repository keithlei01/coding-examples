# Explanation

`Array.prototype.filter` builds a new array with elements where the callback returns true.

**Time:** O(n) · **Space:** O(output size)

In interviews, say you could also stream large files and filter without holding all rows in memory.
