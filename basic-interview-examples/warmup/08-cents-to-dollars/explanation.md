# Explanation

Work on absolute value, format with `(abs / 100).toFixed(2)`, then add `-` before `$` if needed.

**Do not** accumulate money as floats in real systems — keep cents as integers (easy **revenue-by-region**).

**Time:** O(1) · **Space:** O(1)
