# Explanation — Merge Two Sorted Logs

Front-to-back two-pointer merge into a **new** array.

Compare heads, push smaller, advance pointer. Drain leftovers.

**Time:** O(n + m) · **Space:** O(n + m)

Stepping stone: master this, then #16 (same logic, write from the back).
