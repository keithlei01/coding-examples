# Explanation

Add each element to a running total. `reduce((s, n) => s + n, 0)` is the functional equivalent.

**Time:** O(n) · **Space:** O(1)

You will use the same pattern when summing revenue cents or units sold in later problems.
