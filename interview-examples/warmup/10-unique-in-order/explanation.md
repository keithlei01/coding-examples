# Explanation

Walk the array once; `Set` tracks what you already output.

Different from **find duplicates** — here you keep one copy of each value in original order.

**Time:** O(n) · **Space:** O(n)

Relates to DAU (distinct users) in easy/01 — counting unique vs listing unique.
